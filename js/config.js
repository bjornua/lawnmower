require.config({
    map: {
        "*": {
            immutable: "3rdparty/immutable",
            react: "3rdparty/react-with-addons"
        }
    }
});

define(["main"], function () {
    "use strict";
});
