require.config({
    map: {
        "*": {
            immutable: "3rdparty/immutable.min",
            react: "3rdparty/react"
        }
    }
});

define(["main"], function () {
    "use strict";
});
